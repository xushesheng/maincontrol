# 2026-08-08

- 排查开发板运行报错：
  - `insmod driver_amp.ko` 报 `invalid module format`：实测板子为 aarch64 内核，而模块按 `ARCH=arm` 编成 32 位 ARM，架构不匹配。需改 `amp_driver/Makefile` 用 `ARCH=arm64` + `aarch64-linux-gnu-` 工具链重编。
  - `./user_amp: not found`：user_amp 是 32 位 armhf 动态链接，板子根文件系统缺 `/lib/ld-linux-armhf.so.3`，需改 `aarch64` 原生或补 32 位 libc。
  - 已把目标板 64 位 aarch64 环境事实写入项目 MEMORY.md。
  - 待用户确认后修改两个 Makefile（当前未改代码）。
  - 已按方案A改好：driver Makefile 改 `ARCH=arm64` + aarch64 工具链 `CROSS_COMPILE`；user Makefile 改 `CC=aarch64-linux-gnu-gcc`、清掉无用的 `-I/usr/arm-...`。`user_amp` 暂用动态链接（aarch64 buildroot 自带 glibc）。下一步需在 SDK 环境实编并 `insmod`/`dmesg` 验证。
  - **已解决并验证通过**：内核树按 `fmsh_fmqlmp_defconfig` 重做 `modules_prepare`（arm64）后，驱动顺利编出 64 位 `.ko` 并 `insmod` 成功。过程中修了：①`atomic64_t` 未定义（树还是 32 位配置）→ 重做 arm64 准备；②`__cpuc_flush_dcache_area` ARM32 专属 → 删除（ioremap_nocache 不可缓存）；③`signal_pending` 隐式声明 → 补 `#include <linux/sched/signal.h>`。arm32→arm64 移植的传递包含差异已清完。
  - 已把本次「FMQL ARM32→aarch64 内核模块/用户态移植」全流程存为 user-level skill：`fmql-aarch64-kmod-port`（`~/.workbuddy/skills/fmql-aarch64-kmod-port/SKILL.md`），覆盖诊断、Makefile 模板、内核树准备、ARM32 接口清单、验证命令与坑速查，下次同类移植可直接调。

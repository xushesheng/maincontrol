# AGENTS.md

本文件用于约束在本仓库内工作的代理行为。默认适用于整个项目目录。

## 项目背景

本项目是一个 Zynq AMP 场景下的通信工程，主要包含两部分：

- `driver_amp.c`：Linux 内核侧 AMP IPC 驱动
- `user_amp.c`：Linux 用户态转发与控制面处理程序

代理在分析、解释、修改代码时，应优先保持现有架构与协议语义稳定，不要在未说明影响范围的情况下随意调整接口、地址映射、线程模型、报文格式或网络拓扑假设。

## 默认工作方式

- 优先先阅读再动手，不要基于猜测修改代码。
- 涉及驱动、共享内存地址、SGI、中断、TUN、路由、pcap、控制帧格式时，要明确说明改动目的与风险。
- 对硬编码网络参数、物理地址、协议字段、设备名的修改，应先指出上下游影响。
- 若用户要求的是分析、说明、定位问题、给建议、评审或梳理文档，则默认不直接改代码。

## 注释保护规则

- 在进行代码修改时，不要主动删除已有注释内容。
- 只有在“注释对应的代码被一并删除”时，才允许删除对应注释。
- 若注释表述过时，但对应代码仍保留，默认先保留注释，并在给出建议时说明应否修订。

## 审阅确认规则

- 代理在准备修改代码前，应先给出修改建议、修改解释、修改范围和落点文件。
- 必须等待用户完成审阅并明确同意后，才能实际修改代码。
- 如果用户仅要求分析、说明、定位或给建议，则只提供建议，不直接改动文件。
- 若需求明显会影响多个模块，也应先列出将改动的文件，再等待确认。

## 修改建议输出格式

在准备实际改动前，建议至少说明以下内容：

- 修改目标
- 为什么要改
- 计划怎么改
- 影响范围
- 落点文件
- 是否涉及协议、接口、行为变化

如果涉及多个文件，先按文件列出改动点，再等待用户确认。

## 修改边界

- 不要无故重命名现有文件、函数、结构体或宏。
- 不要无故删除调试信息、注释、兼容分支或已有保护逻辑。
- 未经用户确认，不要主动把硬编码逻辑“顺手重构”为配置系统。
- 未经用户确认，不要擅自调整协议格式、寄存器地址、共享内存布局、IP 映射规则。
- 修改时应尽量局部、可追踪、可回退。

## 文档与说明

- 新增 README、设计说明、注释补充时，应以当前仓库真实代码为准，不假设仓库外存在未提供的脚本或工程。
- 若发现代码注释乱码、工程文件缺失、依赖未纳入仓库，应如实说明，不自行脑补缺失内容。

## 适用说明

- 本文件约束仓库内所有代理操作。
- 若用户在当前对话中提出了更严格的要求，以用户要求为准。
